# zizmor policy for jooservices/client workflows.
# Version tags (@v7) are acceptable; SHA pinning is enforced selectively (e.g. SonarCloud on changed lines).
rules:
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
