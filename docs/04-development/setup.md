# Development Setup

## Local Setup

1. `composer install`
2. Optional Docker MongoDB: `docker compose up -d mongodb`
3. Run baseline checks: `composer lint`

## Hooks

Install git hooks with `composer hook:install`.
