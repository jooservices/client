# Secret Scanning

## Tooling

- Gitleaks in CI (`secret-scanning.yml`)
- Optional local scans before push

## Policy

Do not commit credentials, tokens, or private keys. Use environment variables and secret management.
